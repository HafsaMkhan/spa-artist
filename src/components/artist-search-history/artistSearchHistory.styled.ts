import { Container } from 'react-bootstrap';
import styled from 'styled-components';

export const ArtistSearchHistoryStyled = styled(Container)`
  margin: 40px auto;

  .no-results {
    display: flex;
    justify-content: center;
    width: 100%;
  }
`;
